const superHeros: string[] = []
const heroPower: number[] = []

type User = {
    name: string
    isActive: boolean
}

const AllUsers: User[] = []

const MLModels: number[][] = [
    [255, 255, 255],
    []
]

superHeros.push("spiderman")
heroPower.push(100)
AllUsers.push({name: "pragya", isActive: true})